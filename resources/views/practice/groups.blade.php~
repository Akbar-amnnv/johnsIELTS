@extends('layouts.app')

@section('content')
    <div class="max-w-6xl mx-auto py-10 px-6">
        <div class="mb-8 flex items-center justify-between gap-4">
            <div>
                <h1 class="text-3xl font-bold text-gray-800">{{ $typeLabel }}</h1>
                <p class="text-gray-600 mt-2">
                    Choose a practice set from the available question groups.
                </p>
            </div>

            <a href="{{ route('practice.index') }}"
               class="px-4 py-2 bg-gray-200 text-gray-800 rounded-xl hover:bg-gray-300 transition">
                Back
            </a>
        </div>

        @if($groups->isEmpty())
            <div class="bg-white border rounded-2xl shadow p-8 text-center">
                <h2 class="text-xl font-semibold text-gray-800 mb-2">No practice sets found</h2>
                <p class="text-gray-600">
                    There are no question groups for this practice type yet.
                </p>
            </div>
        @else
            <div class="space-y-4">
                @foreach($groups as $group)
                    <div class="bg-white border rounded-2xl shadow p-6">
                        <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-4">
                            <div>
                                <h2 class="text-xl font-semibold text-gray-800">
                                    {{ $group->passage->readingTest->title }}
                                </h2>

                                <p class="text-gray-600 mt-1">
                                    {{ $group->passage->title }}
                                    • Questions {{ $group->start_number }}–{{ $group->end_number }}
                                </p>

                                @if($group->instruction)
                                    <p class="text-sm text-gray-500 mt-2">
                                        {{ $group->instruction }}
                                    </p>
                                @endif
                            </div>

                            <a href="{{ route('practice.show', [$type, $group]) }}"
                               class="inline-block text-center px-5 py-3 bg-blue-600 text-white rounded-xl hover:bg-blue-700 transition">
                                Start This Set
                            </a>
                        </div>
                    </div>
                @endforeach
            </div>
        @endif
    </div>
@endsection