@extends('layouts.app')

@section('content')
    <div class="max-w-5xl mx-auto py-10 px-4">
        <div class="mb-8">
            <a href="{{ route('vocabulary.index') }}" class="text-sm text-gray-500 hover:text-gray-700">
                ← Back to Vocabulary
            </a>

            <h1 class="text-3xl font-bold text-gray-900 mt-2">Quiz Result</h1>
            <p class="text-gray-600 mt-2">
                {{ $attempt->category->name }} • {{ ucfirst($attempt->quiz_type) }} Quiz
            </p>
        </div>

        <div class="grid md:grid-cols-3 gap-4 mb-8">
            <div class="bg-white border rounded-2xl p-6 text-center">
                <p class="text-sm text-gray-500">Score</p>
                <p class="text-3xl font-bold text-amber-600 mt-2">{{ $attempt->score_percent }}%</p>
            </div>

            <div class="bg-white border rounded-2xl p-6 text-center">
                <p class="text-sm text-gray-500">Correct</p>
                <p class="text-3xl font-bold text-green-600 mt-2">{{ $attempt->correct_answers }}</p>
            </div>

            <div class="bg-white border rounded-2xl p-6 text-center">
                <p class="text-sm text-gray-500">Total</p>
                <p class="text-3xl font-bold text-slate-800 mt-2">{{ $attempt->total_questions }}</p>
            </div>
        </div>

        <div class="space-y-4">
            @foreach($attempt->answers as $index => $answer)
                <div class="bg-white border rounded-2xl p-6">
                    <div class="flex items-start justify-between gap-4 mb-4">
                        <div>
                            <p class="text-xs uppercase tracking-wide text-gray-400">
                                Question {{ $index + 1 }}
                            </p>
                            <h2 class="text-lg font-semibold text-gray-900 mt-2">
                                {{ $answer->prompt_text }}
                            </h2>
                        </div>

                        <span class="px-3 py-1 rounded-full text-xs font-semibold {{ $answer->is_correct ? 'bg-green-100 text-green-700' : 'bg-red-100 text-red-700' }}">
                        {{ $answer->is_correct ? 'Correct' : 'Wrong' }}
                    </span>
                    </div>

                    <div class="grid md:grid-cols-2 gap-4 text-sm">
                        <div class="p-4 rounded-xl bg-gray-50">
                            <p class="text-gray-500 mb-1">Your Answer</p>
                            <p class="font-medium text-gray-900">
                                {{ $answer->selected_answer ?: 'No answer selected' }}
                            </p>
                        </div>

                        <div class="p-4 rounded-xl bg-green-50">
                            <p class="text-green-600 mb-1">Correct Answer</p>
                            <p class="font-medium text-green-800">
                                {{ $answer->correct_answer }}
                            </p>
                        </div>
                    </div>

                    @if($answer->vocabulary)
                        <div class="mt-4 p-4 rounded-xl bg-amber-50 border border-amber-100">
                            <p class="text-sm text-amber-700 font-semibold">{{ $answer->vocabulary->word }}</p>
                            <p class="text-sm text-gray-700 mt-1">{{ $answer->vocabulary->translation }}</p>
                            <p class="text-sm text-gray-600 mt-2">{{ $answer->vocabulary->definition }}</p>
                        </div>
                    @endif
                </div>
            @endforeach
        </div>

        <div class="mt-8 flex flex-wrap gap-3">
            <a href="{{ route('vocabulary.translation', $attempt->category) }}"
               class="px-5 py-3 rounded-xl bg-amber-600 text-white font-medium hover:bg-amber-700 transition">
                Try Translation Again
            </a>

            <a href="{{ route('vocabulary.meaning', $attempt->category) }}"
               class="px-5 py-3 rounded-xl bg-slate-900 text-white font-medium hover:bg-slate-800 transition">
                Try Meaning Quiz
            </a>
            <a href="{{ route('vocabulary.index') }}"
               class="px-5 py-3 rounded-xl bg-slate-900 text-white font-medium hover:bg-slate-800 transition">
                Try Meaning Quiz
            </a>
        </div>
    </div>
@endsection